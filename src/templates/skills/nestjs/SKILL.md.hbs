---
name: {{stackName}}
description: Best practices, patterns and commands for {{stackName}} development
---

# NestJS Skill

## Configuration
{{#if version}}
- Version: {{version}}
{{/if}}
{{#if structure.hasTypeScript}}
- TypeScript: enabled
{{/if}}
{{#each evidence}}
- {{this}}
{{/each}}

{{#if (hasItems structure.srcDirs)}}
## Project Structure

```
{{#each structure.srcDirs}}
{{this}}
{{/each}}
```
{{else}}
## Project Structure

```
src/
├── modules/       # Feature modules
├── common/        # Shared utilities, guards, interceptors
├── config/        # Configuration
└── main.ts
```
{{/if}}

{{#if structure.testDir}}
Tests located in: `{{structure.testDir}}/`
{{/if}}

{{#if (hasItems structure.configFiles)}}
## Detected Config Files

{{#each structure.configFiles}}
- `{{this}}`
{{/each}}
{{/if}}

## Best Practices

1. **Module Organization**: Group related features into modules
2. **Dependency Injection**: Use constructor injection for services
3. **DTOs**: Create Data Transfer Objects for validation
4. **Guards**: Implement authentication and authorization
5. **Interceptors**: Handle cross-cutting concerns

## Common Patterns

### Dependency Injection
```typescript
@Injectable()
export class UsersService {
  constructor(
    @InjectRepository(User)
    private usersRepository: Repository<User>,
  ) {}
}
```

### Request Validation with DTOs
```typescript
import { IsString, IsEmail } from 'class-validator';

export class CreateUserDto {
  @IsString()
  name: string;

  @IsEmail()
  email: string;
}
```

## Commands

{{#if (hasItems structure.scripts)}}
```bash
{{#each structure.scripts}}
npm run {{name}}{{#if command}}   # {{command}}{{/if}}
{{/each}}
```
{{else}}
```bash
npm run start:dev    # Development with watch mode
npm run build        # Build for production
npm run test         # Unit tests
npm run test:e2e     # End-to-end tests
npm run test:cov     # Coverage report
```
{{/if}}

```bash
nest generate module <name>      # Generate module
nest generate controller <name>  # Generate controller
nest generate service <name>     # Generate service
```

---
name: {{stackName}}
description: Best practices, patterns and commands for {{stackName}} development
---

# React Skill

## Configuration
{{#if version}}
- Version: {{version}}
{{/if}}
{{#if structure.hasTypeScript}}
- TypeScript: enabled
{{/if}}
{{#each evidence}}
- {{this}}
{{/each}}

{{#if (hasItems structure.srcDirs)}}
## Project Structure

```
{{#each structure.srcDirs}}
{{this}}
{{/each}}
```
{{else}}
## Project Structure

```
src/
├── components/    # Reusable UI components
├── pages/         # Page-level components
├── hooks/         # Custom hooks
├── store/         # State management
└── utils/
```
{{/if}}

{{#if structure.testDir}}
Tests located in: `{{structure.testDir}}/`
{{/if}}

## Best Practices

1. **Component Organization**: Keep components small and focused
2. **State Management**: Use hooks (useState, useReducer) or external libraries (Redux, Zustand)
3. **Performance**: Memo, useMemo, useCallback for optimization
4. **TypeScript**: Prefer TypeScript for type safety
5. **Testing**: Jest + React Testing Library

## Common Patterns

### Functional Component
```typescript
import { useState } from 'react';

function Counter() {
  const [count, setCount] = useState(0);
  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + 1)}>Increment</button>
    </div>
  );
}
```

### Custom Hook
```typescript
function useLocalStorage<T>(key: string, initialValue: T) {
  const [storedValue, setStoredValue] = useState<T>(() => {
    const item = window.localStorage.getItem(key);
    return item ? JSON.parse(item) : initialValue;
  });
  const setValue = (value: T) => {
    setStoredValue(value);
    window.localStorage.setItem(key, JSON.stringify(value));
  };
  return [storedValue, setValue] as const;
}
```

## Commands

{{#if (hasItems structure.scripts)}}
```bash
{{#each structure.scripts}}
npm run {{name}}{{#if command}}   # {{command}}{{/if}}
{{/each}}
```
{{else}}
```bash
npm start          # Development server
npm run build      # Production build
npm test           # Run tests
npm run lint       # Lint
```
{{/if}}
